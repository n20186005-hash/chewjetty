#:schema node_modules/wrangler/config-schema.json
name = "chew-jetty"
compatibility_date = "2025-08-11"

# Deploy output from `pnpm build` to Cloudflare Pages (Workers Static Assets).
pages_build_output_dir = "./dist"
